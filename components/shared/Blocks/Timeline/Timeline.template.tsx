import { Template } from "tinacms";

export const timelineTemplate: Template = {
    name: "Timeline",
    label: "Timeline",
    fields: [
        {
            name: "title",
            label: "Title",
            type: "string",
        },
    ],
};