---
seo:
  title: SSW EagleEye - Manage Database Credentials
  description: >-
    Find EagleEye database usernames and passwords in Key Vault after
    deployment.
title: Manage Database Credentials
date: '2026-03-06T00:00:00.000Z'
---

Use this guide if you need to find current EagleEye database credentials after deployment.

## Required roles and permissions

You need access to the EagleEye managed resource group and Key Vault.

Recommended minimum roles:

* **[Key Vault Secrets User](https://learn.microsoft.com/en-us/azure/role-based-access-control/built-in-roles/security#key-vault-secrets-user)** (or equivalent read access) on the Key Vault

## Find database credentials in Azure Portal

1. In **Azure Portal** -> **Resource groups**, open the resource group used for EagleEye deployment.
2. Open the EagleEye managed application project.

   ![](/EagleEye/Guides/EagleEye-RG.png)
3. Open the **Managed resource group** (for example, `mrg-ssweagleeye-xxx`).

   ![](/EagleEye/Guides/EagleEye-MRG.png)
4. Open the shared Key Vault (for example, `kv-shared-xxx`).

   ![](/EagleEye/Guides/KV-Shared.png)
5. Go to **Secrets** and open the required secret:
   * `Database-ReadOnly-Username`
   * `Database-ReadOnly-Password`
   * `Database-ReadWrite-Username`
   * `Database-ReadWrite-Password`
   * `Database-Admin-Username`
   * `Database-Admin-Password`
   ![](/EagleEye/Guides/DB-Credentials.png)
6. Click the secret name to open its versions.

   ![](/EagleEye/Guides/DB-Password-Version.png)

7. Select the latest (current) version.
8. Click **Show Secret Value** to reveal the value, then copy it as needed (for example, for Power BI setup).

   ![](/EagleEye/Guides/DB-Read-Only-Password.png)

> **Tip:** For Power BI setup, use the `Database-ReadOnly-Username` and `Database-ReadOnly-Password` values.

## Changing database credentials

This guide does not cover changing database usernames/passwords.

> **Important:** Changing a database credential value in Key Vault does not change the underlying Azure SQL user credentials (`Admin`, `ReadWrite`, `ReadOnly`).

Updating values in Key Vault alone does not change SQL logins/passwords in Azure SQL, and changing credentials safely requires coordinated updates across SQL and dependent secrets.

For now, use this guide for **finding** credentials only.

## Related guides

* [Step 3 - Install from Azure Marketplace](./step-3-install-from-marketplace)
* [Step 4 - Integrate Power BI report](./step-4-integrate-power-bi-report)
* [Manage Secrets for App Registrations](./secret-management-app-registrations)
